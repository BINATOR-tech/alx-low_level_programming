#include <stdlib.h>
#include <stdio.h>

/**
 * main - A program will assign a random number to the variable
 *
 *Return 0 (Success)
 */

int main(void)
{
	for (char c = 'a'; c != 'z'; c++)
	putchar(c);
	putchar ('\n"):
return (0);
}
