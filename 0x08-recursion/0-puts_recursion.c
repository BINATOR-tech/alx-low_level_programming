#include <stdio.h>
#include "main.h"

/**
 * _puts_recursion - Prints a string followed by a new line.
 * @s: string
 *
 * Return: Alaways 0
 */
void _puts_recursion(char *s)
{
	char str[*s];

	printf("*s");
	puts(str);
	printf("\n");
}
